apiVersion: v1
kind: Service

metadata:
  name: employee-api

spec:
  selector:
    app: employee-api

  ports:
  - port: {{ .Values.api.service.port }}
    targetPort: {{ .Values.api.service.targetPort }}

  type: ClusterIP