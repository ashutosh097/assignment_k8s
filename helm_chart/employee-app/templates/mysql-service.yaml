apiVersion: v1
kind: Service

metadata:
  name: mysql

spec:
  selector:
    app: mysql

  clusterIP: None

  ports:
    - port: {{ .Values.mysql.service.port }}
      targetPort: {{ .Values.mysql.service.targetPort }}